1.必须将所有图片放在datas文件夹下
2.每当改变了打印机的衰减因子，算法会重新计算LUT，这会花费一会儿（若之前记录过该衰减因子，则不会再计算）
3.半色调的结果，第二张图为使用灰度映射后，打印结果和原图像的SSIM结构性相似图，亮度越高表示该地方越相似，
子图题目为打印结果相对原图像的SSIM指数，越大代表越相似。大标题为半调算法结果对原图像的SSIM指数
4.在命令行中输入mainapp即可运行该gui